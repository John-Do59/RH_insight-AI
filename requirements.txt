# ══════════════════════════════════════════════════════
# requirements.txt  —  PRODUCTION ONLY (used in Docker)
# ══════════════════════════════════════════════════════

# Core API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic-settings>=2.5.0
pydantic[email]>=2.5.0
email-validator>=2.0.0
python-multipart>=0.0.9
python-dotenv>=1.0.0

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
asyncpg>=0.29.0
redis>=5.0.0
alembic>=1.13.0
pgvector

# Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# AI & RAG  (Ollama-only: NO torch, NO sentence-transformers)
langchain-ollama>=0.1.0
langchain-core>=0.3.0
langgraph>=0.2.0
pypdf>=5.0.0
chromadb>=0.5.0
langchain-chroma>=0.1.0
langchain-community>=0.3.0
# NOTE: langchain-huggingface and sentence-transformers are DEV-ONLY.
#       In production EMBEDDING_PROVIDER=ollama → torch is never loaded.
#       See requirements-dev.txt for local dev/fallback.

# Utils
httpx>=0.27.0
loguru>=0.7.0
cachetools>=5.3.0
slowapi==0.1.9

# Observability
langfuse>=2.0.0
